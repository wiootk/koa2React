const config = {
  // 启动端口
  port: 5555, 
  showSql:true,
  // 数据库配置
  database: {
    DATABASE: 'demo2',
    USERNAME: 'root',
    PASSWORD: 'root',
    PORT: '3306',
    HOST: 'localhost'
  }
} 
module.exports = config